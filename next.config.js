/** @type {import('next').NextConfig} */
const nextConfig = {
  // Suas configurações aqui
  // Por exemplo, para Next.js 15 e React 19 (se você decidir mantê-los para testes):
  // experimental: {
  //   reactCompiler: true, // Se você estiver usando o React Compiler
  //   appDir: true,
  // },
  // outras configurações como images, i18n, rewrites etc.
};

module.exports = nextConfig;